<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal status screen shown on the Glass prism (if attached).
     Remove the display module and this is never seen — the service runs headlessly. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#000000"
    android:padding="24dp">

    <TextView
        android:id="@+id/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GlassCam AI"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Initialising…"
        android:textColor="#AAAAAA"
        android:textSize="16sp"
        android:gravity="center" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Tap  →  capture now\nSwipe back  →  pause/resume"
        android:textColor="#666666"
        android:textSize="13sp"
        android:gravity="center" />

</LinearLayout>
